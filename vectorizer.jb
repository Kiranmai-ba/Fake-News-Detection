import os
import joblib

# Get the directory where the current script is located
base_path = os.path.dirname(__file__)
file_path = os.path.join(base_path, 'vectorizer.jb')

model = joblib.load(file_path)
